name: PowerShortcuts iOS CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: power-shortcuts-ios-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build PowerShortcuts
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: List schemes
        run: xcodebuild -list -project PowerShortcuts.xcodeproj

      - name: Build for iOS simulator
        shell: bash
        run: |
          set -o pipefail
          mkdir -p build/power-shortcuts-ci
          set +e
          xcodebuild \
            -project PowerShortcuts.xcodeproj \
            -scheme PowerShortcuts \
            -configuration Debug \
            -sdk iphonesimulator \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | tee build/power-shortcuts-ci/xcodebuild.log
          status=${PIPESTATUS[0]}
          set -e

          if [[ "${status}" -ne 0 ]]; then
            echo "::group::xcodebuild diagnostics"
            grep -Ei "error:|fatal error:|BUILD FAILED|Check dependencies" build/power-shortcuts-ci/xcodebuild.log | tail -80 || true
            echo "::endgroup::"

            while IFS= read -r line; do
              escaped="${line//'%'/'%25'}"
              escaped="${escaped//$'\r'/'%0D'}"
              escaped="${escaped//$'\n'/'%0A'}"
              echo "::error title=Xcode build::${escaped}"
            done < <(grep -Ei "error:|fatal error:|BUILD FAILED|Check dependencies" build/power-shortcuts-ci/xcodebuild.log | tail -20 || true)

            exit "${status}"
          fi
