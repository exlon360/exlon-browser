name: Build Glide Emu IPA

on:
  workflow_dispatch:
  push:
    tags:
      - "emu-v*"

permissions:
  contents: read

jobs:
  unsigned-ipa:
    name: Build unsigned Glide Emu IPA
    runs-on: macos-15-arm64
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Build unsigned IPA
        run: bash scripts/build_glide_emu_unsigned_ipa.sh

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: GlideEmu-unsigned-ipa
          path: build/glide-emu-unsigned-ipa/GlideEmu-unsigned.ipa
          if-no-files-found: error

      - name: Publish IPA to GitHub Release
        if: startsWith(github.ref, 'refs/tags/emu-v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/glide-emu-unsigned-ipa/GlideEmu-unsigned.ipa
          generate_release_notes: true
