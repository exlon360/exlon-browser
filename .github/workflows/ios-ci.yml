name: iOS CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ios-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ZenFireBrowser
    runs-on: macos-15-arm64
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: List schemes
        run: xcodebuild -list -project ZenFireBrowser.xcodeproj

      - name: Build for iOS simulator
        run: |
          set -o pipefail
          xcodebuild \
            -project ZenFireBrowser.xcodeproj \
            -scheme ZenFireBrowser \
            -configuration Debug \
            -sdk iphonesimulator \
            CODE_SIGNING_ALLOWED=NO \
            build
