name: Level Creator iOS CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: level-creator-ios-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Level Creator
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: List schemes
        run: xcodebuild -list -project LevelCreator.xcodeproj

      - name: Build for iOS simulator
        run: |
          set -o pipefail
          xcodebuild \
            -project LevelCreator.xcodeproj \
            -scheme LevelCreator \
            -configuration Debug \
            -sdk iphonesimulator \
            CODE_SIGNING_ALLOWED=NO \
            build
